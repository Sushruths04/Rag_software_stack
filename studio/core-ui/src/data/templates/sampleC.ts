import { DEMO_GRAPH } from "../demoGraph";

/**
 * Sample C — "the full v2 pipeline shape" (BLOCK_GUIDE.md §10). This IS the
 * built-in demo graph (22 blocks / 26 wires) — re-exported, not duplicated,
 * so the template picker and the app's default canvas always agree on the
 * exact same fixture.
 */
export const SAMPLE_C_TEMPLATE = DEMO_GRAPH;
