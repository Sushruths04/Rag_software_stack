"""GRAFT Studio — Phase 2 node-graph builder (backend scaffold).

This namespace package is intentionally separate from ``production/`` and
``src/rag_gt/``. Nothing under ``studio/`` imports the real engine; all block
adapters are stubs until the M0 engine refactor lands (see
``05_BLOCK_CATALOG.md`` §4 in the plan repo).
"""
